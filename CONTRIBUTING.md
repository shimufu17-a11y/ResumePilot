# Contributing to ResumePilot

感谢你帮助 ResumePilot 支持更多招聘网站。

## 开发流程

1. Fork 仓库并创建功能分支。
2. 使用 Node.js 22+ 执行 `npm install`。
3. 修改核心逻辑前先阅读[架构说明](docs/architecture.md)。
4. 使用虚构数据添加或更新测试。
5. 执行 `npm run check`。
6. 在 Pull Request 中说明网站、验证日期、浏览器版本、覆盖字段和已知限制。

## 网站适配要求

- 优先修复通用引擎或底层 ATS 适配器，避免复制企业专用逻辑。
- 不得绕过验证码、人机验证、登录限制或网站安全措施。
- 不得添加自动最终提交能力。
- 适配器失败时必须安全降级到预览或人工操作。
- 只有完成真实页面测试并记录证据范围后，才能把状态改为“已验证”。

## 隐私要求

禁止提交真实简历、身份证号、手机号、邮箱、成绩单、Cookie、访问令牌、API Key 或未经脱敏的网页快照。测试夹具中的姓名、学校、公司和联系方式必须明显为虚构数据。
